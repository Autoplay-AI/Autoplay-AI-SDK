---
title: "Dify tutorial"
sidebarTitle: "Dify tutorial"
description: "Orchestrate real-time events, your Dify chatbot, and visual guidance tours to deliver personalised customer support."
icon: "/images/recipes/dify/logo.png"
---

This tutorial shows you how to connect your existing tools — a session replay provider, a Dify chatbot, and visual guidance tours — into a single personalised customer support experience. Autoplay SDK is the connective tissue that captures what each user is doing in real time and routes that context to the right tool at exactly the right moment.

---

## What you'll orchestrate

<Frame>
  <img src="/images/recipes/dify/autoplay_sdk_connective_tissue.svg" alt="Autoplay SDK as the connective tissue between session replay, Dify chatbot, and visual guidance tours" />
</Frame>

You already have the pieces:

- **Session replay provider** — captures what the user is doing in real time (PostHog or equivalent)
- **Visual guidance tours** — step-by-step flows triggered at the right moment (UserTour or equivalent)
- **Dify chatbot** — the conversational interface that answers questions

**Autoplay SDK** brings them together. It routes live session context into your chatbot so every answer is specific to where that user is — and fires visual tours automatically when they need a guided walkthrough, personalised to their journey.

---

## ✨ Final result

<CardGroup cols={2}>
  <Card>
    <Frame>
      <img src="/images/recipes/dify/step1-final-result.png" alt="The Dify chatbot replies with context from the user's live session, surfacing a Show me how button" />
    </Frame>
    The chatbot replies with context pulled from the user's live session — surfacing the right guidance without being asked.
  </Card>
  <Card>
    <Frame>
      <img src="/images/recipes/dify/tour-final-result-1.png" alt="A visual guidance tour fires automatically, walking the user through the steps they need" />
    </Frame>
    The visual guidance tour fires automatically, walking the user through the exact steps they need.
  </Card>
</CardGroup>

---

## 📋 Prerequisites

Complete the [Quickstart](/quickstart). You should have:

- **PostHog in the browser** — snippet, API key, `product_id` on `identify`
- **Connector credentials** — stream URL and API token from your Autoplay dashboard
- **`autoplay-sdk` installed** — and a successful test stream from the Quickstart
- **A Dify account** — cloud at [cloud.dify.ai](https://cloud.dify.ai) or self-hosted, with an existing chatbot application
- **A public HTTPS URL** — where you can host the event-stream server

---

## The building blocks

Follow the steps in order — each page stands alone so you can ship incrementally.

1. **[Connect real-time events](./step-1-connect-real-time-events)** — Set up the event-stream server, wire the Dify External Knowledge API, and tune background summarization.
2. **[Define proactive triggers](./step-2-define-proactive-triggers)** — Proactively message users in Dify based on what they're doing in your product. *(Coming soon)*
